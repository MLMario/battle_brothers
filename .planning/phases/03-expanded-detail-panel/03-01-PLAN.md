---
phase: 03-expanded-detail-panel
plan: 01
type: execute
requirements: [DATA-03, DATA-04, DATA-05, DATA-06]
wave: 1
depends_on: [02]
files_modified: [app.js, styles.css]
autonomous: true

must_haves:
  truths:
    - "Every .bg-item contains a .bg-panel sibling of .bg-row"
    - "Each .bg-panel has two .panel-badge pills (Level range, Wage) with correct data"
    - "Each .bg-panel has exactly 8 .attr-row children (one per ATTR_KEYS entry)"
    - "Each .attr-row has a label cell, a colored .attr-bar-fill with width = percentile, an .attr-vals cell (avg + min-max), and an .attr-levelup cell"
    - "Level-up cells render correctly for positive, negative, both-null, and partial-null cases"
    - "The CSS max-height: 600px cap on .bg-item.open .bg-panel has been removed"
  artifacts:
    - path: "app.js"
      provides: "Eager panel construction inside buildRow() — panel subtree appended to every .bg-item"
      contains: "panel-badges"
    - path: "styles.css"
      provides: "No hard-coded open max-height — inline style handles it in Plan 02"
  key_links:
    - from: "app.js buildRow()"
      to: ".bg-item > .bg-panel > .bg-panel-inner"
      via: "document.createElement + appendChild"
      pattern: "className\\s*=\\s*['\"]bg-panel['\"]"
    - from: "app.js attr-row loop"
      to: "ATTR_KEYS iteration with pct() + barColor() reuse"
      via: "forEach"
      pattern: "ATTR_KEYS\\.forEach"
---

<objective>
Build the expanded panel DOM eagerly inside `buildRow()` for every background. This plan produces all visible panel content (two badges + 8 attribute rows with color bars, range text, level-up bonuses) but the panel starts collapsed via existing CSS (`.bg-panel { max-height: 0 }`). Animation wiring and click-to-toggle land in Plan 02.

Purpose: Deliver the DATA-03/04/05/06 content surface so Plan 02 only has to wire interactivity. Mirror the mockup structure verbatim per CONTEXT D-06, D-11–D-15.

Output: `app.js` extended so every `.bg-item` contains a populated `.bg-panel`. `styles.css` with the 600px open cap removed per D-08.
</objective>

<execution_context>
@~/.claude/gsd-light/workflows/execute-plan.md
@~/.claude/gsd-light/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/CODEBASE.md
@.planning/phases/03-expanded-detail-panel/03-CONTEXT.md
@app.js
@styles.css
@mockups/design3_accordion.html
@data/backgrounds.json

<interfaces>
Reusable helpers already in app.js (do NOT re-derive):

```js
// Phase 1 export — iteration list for all 8 attribute rows
const ATTR_KEYS = [
  { key: 'hitpoints',     label: 'HP'  },
  { key: 'meleeSkill',    label: 'MSk' },
  { key: 'rangedSkill',   label: 'RSk' },
  { key: 'meleeDefense',  label: 'MDf' },
  { key: 'rangedDefense', label: 'RDf' },
  { key: 'fatigue',       label: 'Fat' },
  { key: 'resolve',       label: 'Res' },
  { key: 'initiative',    label: 'Ini' },
];

// Phase 2 D-05 — reuse verbatim for .attr-bar-fill width + color
function pct(key, val) { /* returns 0-100 percentile */ }
function barColor(p)   { /* returns 'rgb(r,g,b)' red→yellow→green */ }
```

Data shape per background (from data/backgrounds.json):
```
{
  id, name, icon, baseWage,
  startingLevel: { min, max },
  attributes: {
    hitpoints: { range: {min, max}, average, levelUp: {min, max} | null-sides },
    ... (8 total)
  }
}
```

Mockup panel builder reference: mockups/design3_accordion.html lines 707-793.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove hardcoded max-height open rule (D-08)</name>
  <read_first>styles.css</read_first>
  <files>styles.css</files>
  <action>Delete the rule block `.bg-item.open .bg-panel { max-height: 600px; }` (currently lines 281-283 per CONTEXT canonical_refs). Keep the base `.bg-panel { max-height: 0; ... }` rule (lines 273-279) intact — this still provides the correct initial collapsed state before any JS runs. Keep the transition rule verbatim per D-09. Do NOT touch any other CSS. Why: inline `style.maxHeight = scrollHeight + 'px'` in Plan 02 overrides the CSS for open panels and future-proofs against content growth (D-07).</action>
  <verify>
    <automated>grep -n "600px" styles.css returns no matches inside a .bg-item.open .bg-panel block</automated>
  </verify>
  <done>The `.bg-item.open .bg-panel { max-height: 600px }` rule is deleted. The base `.bg-panel { max-height: 0; overflow: hidden; transition: max-height .32s cubic-bezier(.4,0,.2,1); ... }` rule is unchanged. No other CSS modified.</done>
</task>

<task type="auto">
  <name>Task 2: Extend buildRow() to append populated .bg-panel (D-06, D-11 — D-15)</name>
  <read_first>app.js, mockups/design3_accordion.html (lines 707-793)</read_first>
  <files>app.js</files>
  <action>
Inside `buildRow(bg)` in app.js, after the existing `item.appendChild(row)` line (currently line 264), insert panel construction before the `return item` statement. Factor into helper functions for readability (Claude's discretion per CONTEXT):

1. Create `buildPanel(bg)` that returns the `.bg-panel` element containing a `.bg-panel-inner` wrapper.
2. Inside the inner wrapper, append a `.panel-badges` div. Per D-11, populate it via `innerHTML` with the two fixed-template pills verbatim from the mockup:
   ```
   <div class="panel-badge">Level <span>${bg.startingLevel.min}–${bg.startingLevel.max}</span></div>
   <div class="panel-badge">Wage <span>${bg.baseWage}g</span></div>
   ```
   Use an en-dash (–, U+2013), not a hyphen, to match the mockup.
3. Append a `.attr-list` div. Iterate `ATTR_KEYS` (D-12) and for each `{key, label}` call `buildAttrRow(bg, key, label)`.
4. `buildAttrRow(bg, key, label)` returns an `.attr-row` with four children, created via `document.createElement` (not innerHTML, except where noted):
   - `.attr-label` textContent = label
   - `.attr-bar-wrap` containing `.attr-bar-fill` with `style.cssText = 'width:' + p + '%;background:' + color + ';'` where `p = pct(key, attr.average)` and `color = barColor(p)` (D-13 — reuse helpers, do NOT re-derive).
   - `.attr-vals` populated via `innerHTML` verbatim from the mockup (D-14): `<span class="attr-avg">${avg}</span> <span style="color:#555;font-size:9px">${rangeMin}–${rangeMax}</span>`. When `attr.range?.min` or `attr.range?.max` is null/undefined, substitute `'?'`.
   - `.attr-levelup` populated per D-15:
       * Both `levelUp.min` and `levelUp.max` null → `textContent = '—'` (em-dash, U+2014), no class added. Do NOT hide the element.
       * Otherwise compute `luMinStr = luMin !== null ? luMin : '?'` and `luMaxStr = luMax !== null ? luMax : '?'`, pick `val = luMin ?? luMax`.
       * If `val > 0` → textContent = `` `+${luMinStr}/+${luMaxStr}` ``, add `.positive` class (green).
       * If `val < 0` → textContent = `` `${luMinStr}/${luMaxStr}` `` (no `+` prefix — negative sign inherent in value), add `.negative` class (red).
   - Append all four children in the order: label, bar-wrap, vals, levelup.
5. If `bg.attributes[key]` is missing, skip that attribute row (matches mockup line 729: `if (!attr) return;`).
6. Do NOT touch the existing `row.addEventListener('click', ...)` stub — Plan 02 replaces its body. Keep the `console.log('[click]', bg.id);` line intact for now (Plan 02 removes it).
7. Append the built panel to `item` (so structure is `item > [row, panel]`) before `return item`.

Why eager (not lazy): 67 panels × ~40 nodes = ~2700 nodes total, trivial on a 430px mobile viewport. No hydration flag needed per D-06.
  </action>
  <verify>
    <automated>Open index.html in a browser and run in DevTools console: `document.querySelectorAll('.bg-panel').length === 67 && document.querySelectorAll('.panel-badge').length === 134 && document.querySelectorAll('.attr-row').length >= 67*8 - 5` (the subtraction allows for a few missing-attribute skips). All .attr-bar-fill elements have non-empty inline width. Verify by eye: first row (Adventurous Noble or first in data) panel shows 2 badges + 8 attribute rows with colored bars. No JS errors in console.</automated>
  </verify>
  <done>
- 67 `.bg-panel` elements exist in the DOM, one per `.bg-item`.
- Each panel contains exactly 2 `.panel-badge` pills (Level, Wage) with correct values from the data.
- Each panel contains up to 8 `.attr-row` children (fewer only if a background lacks an attribute object).
- `.attr-bar-fill` elements have inline `width:` and `background:` styles derived from `pct()` and `barColor()`.
- `.attr-vals` shows avg + min–max (or `?` for missing).
- `.attr-levelup` correctly renders `+/+` positive (green), raw negative (red), `—` for both-null, `?` for partial-null.
- Panels are visually collapsed (max-height: 0 from base CSS) — no animation wiring yet.
- Existing click-handler stub at `row.addEventListener('click', ...)` is untouched.
- No console errors on page load.
  </done>
</task>

</tasks>

<verification>
1. `document.querySelectorAll('.bg-panel').length === 67`
2. `document.querySelectorAll('.panel-badge').length === 134`
3. Manually expand a panel in DevTools by adding inline `style="max-height: 1000px"` — confirm 2 badges + 8 attribute rows render with color-coded bars, range text, and level-up text.
4. Spot-check a background with a null `levelUp` side to confirm `?` substitution. Spot-check one with both-null to confirm `—`.
5. `grep -n "600px" styles.css` returns nothing inside `.bg-item.open .bg-panel`.
6. Confirm no regression: collapsed rows still render icon, name, sparkline, wage, chevron.
</verification>

<success_criteria>
- All 67 `.bg-panel` subtrees built eagerly during `buildRow()`.
- Panel content matches the mockup verbatim (badges innerHTML template, attr-vals innerHTML template, level-up formatting logic).
- `pct()` and `barColor()` reused — no re-derivation.
- CSS 600px cap removed; base `max-height: 0` rule preserved.
- Phase 2 behavior (collapsed row display) unaffected.
</success_criteria>

<output>
After completion, create `.planning/phases/03-expanded-detail-panel/03-01-SUMMARY.md`
</output>
