---
phase: 01-foundation-data-loading
plan: 01
type: execute
requirements: [DATA-01]
must_haves:
  truths:
    - "Repo has a .gitignore excluding Claude local settings and OS junk files"
    - "Repo has a README documenting local dev (python -m http.server) and a live-URL placeholder"
    - "assets/icons/Background 70.png is renamed to assets/icons/Background_70.png on disk"
    - "data/backgrounds.json entry for that icon reads icons/Background_70.png (no space)"
    - "Git working tree is clean after the rename and doc additions are committed"
  artifacts:
    - path: ".gitignore"
      provides: "Ignore rules for local settings and OS artifacts"
      contains: ".claude/settings.local.json"
    - path: "README.md"
      provides: "Project overview, local dev instructions, live-URL placeholder, PROJECT.md link"
      min_lines: 15
    - path: "assets/icons/Background_70.png"
      provides: "Renamed Anatomist icon with no space in filename"
    - path: "data/backgrounds.json"
      provides: "Updated icon path icons/Background_70.png"
      contains: "Background_70.png"
  key_links:
    - from: "data/backgrounds.json"
      to: "assets/icons/Background_70.png"
      via: "icon field for the Anatomist background entry"
      pattern: "icons/Background_70\\.png"
---

<objective>
Close the two infrastructure gaps that make the later plans safe to execute: (1) eliminate the only filename-with-space in the icon set so URL encoding cannot bite us on GitHub Pages, and (2) add the minimal repo hygiene artifacts (`.gitignore`, `README.md`) that the deploy at end of phase depends on.

Purpose: Stable asset paths and a documented local-dev workflow before any production code is written.
Output: Renamed icon file, updated JSON path, `.gitignore`, `README.md`.
</objective>

<execution_context>
@~/.claude/gsd-light/workflows/execute-plan.md
@~/.claude/gsd-light/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-data-loading/01-CONTEXT.md
@.planning/CODEBASE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename Background 70 icon and update JSON reference</name>
  <read_first>.planning/phases/01-foundation-data-loading/01-CONTEXT.md, data/backgrounds.json (search for "Background 70")</read_first>
  <files>assets/icons/Background_70.png (renamed from "Background 70.png"), data/backgrounds.json</files>
  <action>Implements D-09. Use `git mv "assets/icons/Background 70.png" "assets/icons/Background_70.png"` so git tracks the rename as a rename (not delete+add). Then edit data/backgrounds.json: change the single occurrence `"icon": "icons/Background 70.png"` to `"icon": "icons/Background_70.png"`. Do not touch any other icon paths — they already use underscores or have no separator. Do not rename any other file. Do not run `git commit` — the orchestrator handles commits.</action>
  <verify>
    <automated>ls "assets/icons/Background_70.png" && ! ls "assets/icons/Background 70.png" 2>/dev/null && grep -c "Background 70.png" data/backgrounds.json | grep -q "^0$" && grep -c "Background_70.png" data/backgrounds.json | grep -q "^1$"</automated>
  </verify>
  <acceptance_criteria>File `assets/icons/Background_70.png` exists; file `assets/icons/Background 70.png` does not exist; `data/backgrounds.json` contains exactly one occurrence of `Background_70.png` and zero occurrences of `Background 70.png`.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add .gitignore and README at repo root</name>
  <read_first>.planning/PROJECT.md, .planning/phases/01-foundation-data-loading/01-CONTEXT.md</read_first>
  <files>.gitignore, README.md</files>
  <action>Implements D-20 and D-21. Create `.gitignore` with exactly these entries (one per line, newline-terminated): `.claude/settings.local.json`, `.DS_Store`, `Thumbs.db`, `node_modules/`. Do not add anything else — the deploy in Phase 1 publishes from repo root and a broader .gitignore could hide needed files. Create `README.md` (~20 lines, do not exceed 30) covering: one-line project description ("Mobile-first reference for all 67 Battle Brothers backgrounds"), a "Run locally" section with the exact command `python -m http.server 8000` and URL `http://localhost:8000` (per D-17), a "Live demo" section with an explicit TODO placeholder (e.g. `_Live URL: TODO — fill in after first GitHub Pages deploy_`) that Plan 3's deploy checkpoint will update, and a final line linking to `.planning/PROJECT.md`. Do NOT include: install instructions, framework/dep list, build steps — there are none (per PROJECT.md constraint "static frontend only — no build tools"). Do not mention Node, npm, or any package manager. Prose only, no code fences around the http.server command is fine.</action>
  <verify>
    <automated>test -f .gitignore && test -f README.md && grep -q "^.claude/settings.local.json$" .gitignore && grep -q "^.DS_Store$" .gitignore && grep -q "^Thumbs.db$" .gitignore && grep -q "^node_modules/$" .gitignore && grep -qi "python -m http.server" README.md && grep -q "PROJECT.md" README.md && [ "$(wc -l < README.md)" -le 30 ]</automated>
  </verify>
  <acceptance_criteria>.gitignore contains the four required entries and only those; README.md exists at repo root, is at most 30 lines, mentions `python -m http.server`, includes a live-URL placeholder, and links to `.planning/PROJECT.md`.</acceptance_criteria>
</task>

</tasks>

<verification>
- `ls assets/icons/` shows `Background_70.png` (no space) and no `Background 70.png`.
- `grep "Background 70" data/backgrounds.json` returns nothing.
- `cat .gitignore` shows exactly four lines matching D-20.
- Opening README.md shows local-dev instructions and a placeholder for the live URL.
</verification>

<success_criteria>
- D-09 satisfied: icon renamed on disk AND in JSON.
- D-20 satisfied: minimal .gitignore present.
- D-21 satisfied: README covers what/run/live-URL/PROJECT.md link in ~20 lines.
- No production code created (index.html / styles.css / app.js are Plan 2 and 3 scope).
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-data-loading/01-01-SUMMARY.md` recording: files created, files renamed, and any deviations from D-09/D-20/D-21.
</output>
