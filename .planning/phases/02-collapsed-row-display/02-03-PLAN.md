---
phase: 02-collapsed-row-display
plan: 03
type: execute
requirements: [DATA-02, VISU-03]
must_haves:
  truths:
    - "All five ROADMAP Phase 2 success criteria are visually confirmed on the live GitHub Pages site on a real mobile device"
    - "The icon fallback (VISU-03) fires visibly when an icon URL is made to 404 via DevTools"
    - "Zero red console errors on page load or row click"
  artifacts:
    - path: ".planning/phases/02-collapsed-row-display/02-03-SUMMARY.md"
      provides: "Verification log of the five success criteria and the DevTools fallback test"
      min_lines: 20
  key_links:
    - from: "live site (https://mlmario.github.io/battle_brothers/)"
      to: "ROADMAP.md Phase 2 success criteria #1-#5"
      via: "manual visual verification per criterion"
      pattern: "Phase 2"
---

<objective>
Human-verify Plan 02-01 and 02-02's output on the live GitHub Pages site against the ROADMAP's five Phase 2 success criteria, including a DevTools-driven test of the VISU-03 icon fallback (D-14).

Purpose: Prove the phase deliverable is real — the mockup-parity row layout renders correctly across mobile viewports, the sparkline encodes information at a glance, and the fallback path is genuinely wired (not just present in code).
Output: Signed-off SUMMARY documenting pass/fail per criterion; any regression is surfaced as a gap for replanning.
</objective>

<execution_context>
@~/.claude/gsd-light/workflows/execute-plan.md
@~/.claude/gsd-light/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-collapsed-row-display/02-CONTEXT.md
@.planning/phases/02-collapsed-row-display/02-01-PLAN.md
@.planning/phases/02-collapsed-row-display/02-02-PLAN.md
@app.js
@styles.css
@mockups/design3_accordion.html
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Live-site verification of Phase 2 success criteria + icon-fallback DevTools test</name>
  <what-built>
Plan 02-01 added the reusable color/percentile helpers and populated 8-bar sparklines on every collapsed row. Plan 02-02 rewrote `buildRow` into the mockup's nested shape with real icons (36x36, lazy, fallback-wired), wage badges (`${baseWage}g`), and a static `▾` chevron. Skeleton rows updated to match. No CSS changes — styles.css already contained mockup-parity selectors from Phase 1.
  </what-built>
  <how-to-verify>
Open https://mlmario.github.io/battle_brothers/ on a real mobile device (or Chrome DevTools iPhone 14 Pro viewport at 430px) after the Plan 02-02 commit is pushed to `main` and GitHub Pages has rebuilt (allow ~1-2 minutes).

**ROADMAP Phase 2 Success Criterion #1 — Icons:**
1. Scroll the list and confirm every visible row has an icon image at 36x36 with visible 6px rounded corners.
2. Right-click any icon -> Inspect -> confirm `width: 36px; height: 36px; border-radius: 6px;` in computed styles.
3. Spot-check 3 rows against the mockup: Adventurous Noble (Background_06.png), Barbarian (Barbarian.png), and any `Background 70.png` row (the one with the space in the filename) — confirm all three icons display, not the fallback.

**ROADMAP Phase 2 Success Criterion #2 — Icon fallback (VISU-03, D-14):**
1. Open DevTools -> Network tab -> right-click any icon request -> "Block request URL" -> reload.
2. Confirm the affected row now shows the sword-emoji (⚔️) fallback inside a 36x36 rounded frame, not a broken image.
3. Unblock the URL and reload — icon should return.
4. **Do not commit any broken path** — D-14 explicitly prohibits this. The test is DevTools-only.

**ROADMAP Phase 2 Success Criterion #3 — Sparklines:**
1. Every row should show 8 small colored bars between the name and the wage badge.
2. Bar heights should vary — find a row with a mix of strong/weak stats (e.g. any "Noble" background tends to have high resolve/low fatigue, yielding tall green + short red bars).
3. Open console: `document.querySelectorAll('.bg-item .spark-bar').length` should return 536 (67 rows × 8 bars).
4. Spot-check one bar's inline style: should read e.g. `height: 11px; background: rgb(180,170,30);` — the exact numbers depend on percentile, but the pattern must be `height:Npx;background:rgb(r,g,b)`.

**ROADMAP Phase 2 Success Criterion #4 — Wage badges:**
1. Every row should show an amber pill on the right with text like `25g`, `3g`, `14g`.
2. Open any one row in DevTools, find `.wage-badge`, and confirm its textContent matches that background's `baseWage` field in `data/backgrounds.json`.
3. Console sanity check: `document.querySelectorAll('.wage-badge').length === 67`.

**ROADMAP Phase 2 Success Criterion #5 — Chevron:**
1. Every row should show a small `▾` (down-triangle) on the far right.
2. Click a row — it should still log `[click] <bg_id>` to the console (Phase 1 plumbing preserved).
3. Confirm chevron does NOT rotate on click — D-09 says chevron is static until Phase 3.
4. Console sanity check: `document.querySelectorAll('.chevron').length === 67`.

**Mobile-viewport layout:**
1. At 430px width, rows should not horizontally scroll. Icon + name + sparkline + wage + chevron should all fit in the row height (~54px).
2. Name ellipsizes if it's too long (test: "Killer on the Run" may be longer than the available width).
3. Scroll the full list — should be smooth, no layout jank.

**Regression gate:**
1. Zero red console errors on load.
2. Fetch still works: Network tab shows `backgrounds.json` returning 200.
3. Error-state retry still works: offline + reload -> amber Retry button -> online + click Retry -> 67 rows render.
  </how-to-verify>
  <resume-signal>
Type "approved" to finalize Phase 2, or describe any failure per criterion (e.g. "icons 36x36 but sparkline only shows 4 bars on row X") so Plan 02-01 or 02-02 can be revised.
  </resume-signal>
  <acceptance_criteria>
    - All five ROADMAP Phase 2 success criteria pass on the live site on a 430px mobile viewport
    - Icon fallback verified via DevTools URL block (not by committing a broken path)
    - `.spark-bar` count is exactly 536; `.wage-badge` count is exactly 67; `.chevron` count is exactly 67
    - Zero red console errors; click-log still fires; error/retry path still works
    - User types "approved" OR provides a specific failure description for revision
    - A SUMMARY file at `.planning/phases/02-collapsed-row-display/02-03-SUMMARY.md` records each criterion's PASS/FAIL with the evidence (screenshot or console output), including the DevTools fallback test outcome
  </acceptance_criteria>
</task>

</tasks>

<verification>
The checkpoint itself IS the verification. The SUMMARY must record pass/fail per criterion with supporting evidence (console outputs, screenshots if failures, specific counts).
</verification>

<success_criteria>
1. User approves after verifying on real mobile viewport
2. All five ROADMAP Phase 2 success criteria are marked PASS in the SUMMARY with evidence
3. Any failure produces a specific revision target (plan number + criterion number), not a vague "something's off"
</success_criteria>

<output>
After completion, create `.planning/phases/02-collapsed-row-display/02-03-SUMMARY.md` using the template, including:
- PASS/FAIL per ROADMAP Phase 2 success criterion #1-#5
- DevTools fallback test outcome (URL blocked -> fallback visible -> URL unblocked -> icon restored)
- Console counts for `.spark-bar`, `.wage-badge`, `.chevron`
- Mobile viewport notes (any layout issues at 430px)
- User resume-signal text (verbatim)
</output>
