---
phase: 06-sort-pills-direction
plan: 01
type: execute
requirements: [SRCH-02-c1]
wave: 1
depends_on: []
files_modified:
  - index.html
  - app.js
  - styles.css
autonomous: true
must_haves:
  truths:
    - "A horizontal row of 10 pill buttons (Name, Wage, HP, MSk, RSk, MDf, RDf, Fat, Res, Ini) is visible below the search input inside the sticky #controls container."
    - "On first paint (before any JS runs beyond the existing pipeline), the Name pill renders with the .active class, aria-pressed=\"true\", and a ▲ arrow glyph inside a <span class=\"arrow\"> child."
    - "The other 9 pills render as <button class=\"pill\" data-key=\"…\" aria-pressed=\"false\">…</button> with NO .active class and NO <span class=\"arrow\"> child."
    - "#pills-wrap provides horizontal overflow scroll on narrow viewports; the scrollbar is hidden via the existing styles.css rule."
    - "Module-scope state `let sortKey = 'name';` and `let sortAsc = true;` are declared in app.js after the Phase 5 search-state additions, ready for Plan 06-02 to consume."
    - "A single new CSS rule `.pill:focus-visible { outline: 2px solid var(--amber); outline-offset: 2px; }` ships in styles.css; no other CSS edits."
    - "The page still loads, renders all 67 backgrounds via the existing Phase 5 applyFilter pipeline, and no JS errors appear in the console."
  artifacts:
    - path: "index.html"
      provides: "#pills-wrap > #pills inserted inside #controls, AFTER #search-wrap; 10 <button class=\"pill\" data-key=\"…\"> children with Name-active preset"
      contains: "id=\"pills\""
    - path: "app.js"
      provides: "Module-scope state additions: `let sortKey = 'name';` and `let sortAsc = true;` after the Phase 5 D-19 block; no behavior change yet"
      contains: "let sortKey"
    - path: "styles.css"
      provides: "Single new rule `.pill:focus-visible { outline: 2px solid var(--amber); outline-offset: 2px; }` per D-18/D-27"
      contains: ".pill:focus-visible"
  key_links:
    - from: "index.html #controls"
      to: "#pills-wrap (new)"
      via: "sibling placement AFTER #search-wrap (D-07)"
      pattern: "id=\"search-wrap\"[\\s\\S]*?id=\"pills-wrap\""
    - from: "#pills-wrap > #pills"
      to: "styles.css #pills-wrap, #pills, .pill, .pill.active, .pill .arrow (already shipped, dormant)"
      via: "id/class selector match — activates sticky/scroll/pill CSS"
      pattern: "#pills-wrap \\{"
    - from: "app.js IIFE state block"
      to: "Plan 06-02 applySort()"
      via: "module-scope `sortKey` and `sortAsc` vars consumed by Plan 06-02's pipeline"
      pattern: "let (sortKey|sortAsc)"
    - from: "Name pill hard-coded .active + aria-pressed=true + <span class=\"arrow\">▲</span>"
      to: "D-19 initial state (sortKey='name', sortAsc=true)"
      via: "first-paint match — eliminates flicker per D-10"
      pattern: "class=\"pill active\" data-key=\"name\""
---

<objective>
Land the `#pills-wrap > #pills` DOM scaffold inside `#controls` in `index.html`, reserve the two new module-scope state variables (`sortKey`, `sortAsc`) in `app.js`, and add the single new `.pill:focus-visible` CSS rule. This is the structural prerequisite for Plan 06-02, which wires the sort pipeline, pill-click handler, and dev hook.

Purpose: Activate the dormant pill CSS in `styles.css` (lines 96–140, already shipped Phase 1) by introducing the matching DOM, preset the Name-active first-paint state so users see the correct visual without waiting for JS, and reserve the JS state slots Plan 06-02 will consume. Mockup-parity discipline governs every attribute and ordering decision (mockup lines 468–478 verbatim).

Output: Updated `index.html` with a new `#pills-wrap > #pills` block containing 10 `<button class="pill" data-key="…">` entries (Name pill preset active); updated `app.js` with two new `let` declarations under a Phase-6 comment header; updated `styles.css` with a single new `.pill:focus-visible` rule.
</objective>

<execution_context>
@~/.claude/gsd-light/workflows/execute-plan.md
@~/.claude/gsd-light/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/CODEBASE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-sort-pills-direction/06-CONTEXT.md
@index.html
@app.js
@styles.css
@mockups/design3_accordion.html

<interfaces>
<!-- Key contracts the executor needs. Extracted from the shipped code and the mockup reference. -->
<!-- Use these literally — no codebase exploration needed. -->

From index.html (CURRENT shipped DOM after Phase 5 — lines 16–24 show `#app > #controls > #search-wrap` and the other `#app` children):
```html
<div id="app">
  <div id="controls">
    <div id="search-wrap">
      <svg id="search-icon" … >…</svg>
      <input id="search" type="search" …>
    </div>
  </div>                          <!-- #controls currently CLOSES here after #search-wrap -->
  <div id="result-count"></div>
  <main id="list" role="list" aria-label="Backgrounds"></main>
  <div id="empty"> … </div>
  <nav id="bottom-nav"> … </nav>
</div>
```
The new `#pills-wrap > #pills` block MUST be inserted as a SIBLING of `#search-wrap` INSIDE `#controls`, AFTER `#search-wrap` and BEFORE `#controls`'s closing `</div>` (D-07 — `#controls` remains the sole sticky container).

From mockups/design3_accordion.html lines 466–478 (verbatim source for the pills subtree — D-07, D-08, D-09, D-10):
```html
<div id="pills-wrap">
  <div id="pills">
    <button class="pill active" data-key="name">Name <span class="arrow">▲</span></button>
    <button class="pill" data-key="baseWage">Wage</button>
    <button class="pill" data-key="hitpoints">HP</button>
    <button class="pill" data-key="meleeSkill">MSk</button>
    <button class="pill" data-key="rangedSkill">RSk</button>
    <button class="pill" data-key="meleeDefense">MDf</button>
    <button class="pill" data-key="rangedDefense">RDf</button>
    <button class="pill" data-key="fatigue">Fat</button>
    <button class="pill" data-key="resolve">Res</button>
    <button class="pill" data-key="initiative">Ini</button>
  </div>
</div>
```

ACCESSIBILITY ADDITION (D-14): Add `aria-pressed="true"` to the Name pill and `aria-pressed="false"` to the other 9 pills. The mockup does NOT have `aria-pressed` — this is the one intentional deviation, justified by D-14 since these are NEW DOM elements (the "no a11y refactor of existing v1 DOM" exclusion does not apply). Do NOT add per-pill `aria-label` (D-15).

ARROW GLYPH (D-29): The Name pill's `<span class="arrow">▲</span>` uses the literal Unicode character `▲` (U+25B2 BLACK UP-POINTING TRIANGLE). Do NOT use HTML entities (`&#9650;`, `&#x25B2;`) and do NOT use a CSS `content:` pseudo-element.

From app.js lines 4–15 (CURRENT module state block after Phase 5 — insertion target for D-19):
```javascript
  // ── Module state ─────────────────────────────────────────────
  // Phase 6 state (sortKey, sortAsc) intentionally absent.    ← update this comment optionally (see action below)
  let allBgs = [];
  let globalMin = 0;
  let globalMax = 0;
  let globalMinByAttr = {};
  let globalMaxByAttr = {};
  let openId = null;

  // ── Phase 5 D-19: search state (consumed by applyFilter in 05-02) ──
  let filtered = [];
  let query = '';
```
Append `let sortKey = 'name';` and `let sortAsc = true;` immediately AFTER `let query = '';` (line 15), under a new comment header following the `// ── Phase 6 D-NN: … ──` convention (D-19, D-20). Do NOT add `__setSort`, `sortValue`, or `applySort` — those belong to Plan 06-02.

From styles.css lines 96–140 (dormant pill rules that activate the moment the DOM lands — no CSS changes required for baseline, per CODEBASE.md §4.2):
- `#pills-wrap` (lines 96–102) — `overflow-x: auto; scrollbar-width: none;` + webkit scrollbar-hide
- `#pills` (lines 104–108) — `display: flex; width: max-content; gap: 6px; padding: 0 12px 10px;`
- `.pill` (lines 110–126) — pill shape, transitions
- `.pill:active` (line 128)
- `.pill.active` (lines 130–134) — amber fill state: `background: var(--amber-dim); border-color: var(--amber); color: var(--amber);`
- `.pill .arrow` (lines 136–140) — 9px font arrow span

From styles.css `:root` (lines 3–10) — confirmed tokens available:
```css
:root {
  …
  --amber:     #d4a843;
  --amber-dim: #9a7520;
  …
}
```
Use `var(--amber)` (NOT `var(--amber-dim)`) for the focus-visible outline per D-18.

NEW CSS RULE (D-18, D-27) — exactly ONE new rule in styles.css:
```css
.pill:focus-visible { outline: 2px solid var(--amber); outline-offset: 2px; }
```
Insertion point: immediately after the `.pill .arrow { … }` rule (styles.css line 140) — keeps all pill-related selectors contiguous. Do NOT add any other CSS rule this plan (no `:root` token additions, no `#pills` tweaks, no `.pill:hover` additions).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert #pills-wrap > #pills DOM into index.html inside #controls</name>
  <read_first>
    - index.html (full file, lines 1–54)
    - mockups/design3_accordion.html lines 466–478 (source-of-truth for the pills subtree)
    - styles.css lines 96–140 (dormant pill selectors that will activate)
    - .planning/phases/06-sort-pills-direction/06-CONTEXT.md (D-07, D-08, D-09, D-10, D-14, D-15, D-29)
  </read_first>
  <files>index.html</files>
  <action>
Insert a new `#pills-wrap` block as a SIBLING of `#search-wrap` INSIDE `#controls`, immediately AFTER `#search-wrap`'s closing `</div>` and BEFORE `#controls`'s closing `</div>` (per D-07 — `#controls` remains the sole sticky container; `#pills-wrap` provides horizontal overflow scroll).

The block MUST be exactly (preserve 6-space indent to match `#search-wrap`'s sibling level under `#controls` — verify against the existing `#search-wrap` indentation in index.html):

```html
      <div id="pills-wrap">
        <div id="pills">
          <button class="pill active" data-key="name" aria-pressed="true">Name <span class="arrow">▲</span></button>
          <button class="pill" data-key="baseWage" aria-pressed="false">Wage</button>
          <button class="pill" data-key="hitpoints" aria-pressed="false">HP</button>
          <button class="pill" data-key="meleeSkill" aria-pressed="false">MSk</button>
          <button class="pill" data-key="rangedSkill" aria-pressed="false">RSk</button>
          <button class="pill" data-key="meleeDefense" aria-pressed="false">MDf</button>
          <button class="pill" data-key="rangedDefense" aria-pressed="false">RDf</button>
          <button class="pill" data-key="fatigue" aria-pressed="false">Fat</button>
          <button class="pill" data-key="resolve" aria-pressed="false">Res</button>
          <button class="pill" data-key="initiative" aria-pressed="false">Ini</button>
        </div>
      </div>
```

Critical discipline (per D-07..D-10, D-14, D-15, D-29):
- Pill ORDER is EXACTLY: Name, Wage, HP, MSk, RSk, MDf, RDf, Fat, Res, Ini. No reordering. Matches SC-1 and mockup order.
- Pill element type is `<button>` (NOT `<div>`, NOT `<span>`, NOT `<a>`) per D-09 — preserves free keyboard activation (Enter / Space) and focus ring without extra ARIA.
- `data-key` values are EXACTLY: `name`, `baseWage`, `hitpoints`, `meleeSkill`, `rangedSkill`, `meleeDefense`, `rangedDefense`, `fatigue`, `resolve`, `initiative`. These are the exact strings consumed by `sortValue()` in Plan 06-02 (D-09). Do NOT capitalize, do NOT camelCase differently.
- Name pill ships with initial HTML preset per D-10: `class="pill active"` + `aria-pressed="true"` + a `<span class="arrow">▲</span>` CHILD (literal U+25B2 Unicode character, NOT HTML entity, NOT CSS pseudo-element — D-29). First paint shows the correct active pill + glyph without waiting for JS.
- Other 9 pills preset per D-10: `class="pill"` (NO `.active`) + `aria-pressed="false"` + NO `<span class="arrow">` child. Text content is the label only (no arrow space, no arrow glyph).
- Every pill carries `aria-pressed="true|false"` (D-14) — required because these are NEW DOM. The mockup does NOT have it; this is an intentional deviation justified by D-14.

Do NOT add:
- Per-pill `aria-label` (D-15 — visible button text + `aria-pressed` already conveys label + state to screen readers).
- Any custom `tabindex` attribute — `<button>` is focusable by default (D-17).
- Any `onclick=` inline handler — delegated handler is wired in Plan 06-02 (D-11).
- Any wrapper divs, headers, or labels not shown above.
- Any `aria-live` on `#result-count` (out of scope — see CONTEXT D-16).

Indentation: Match the existing `#search-wrap` indentation inside `#controls` (currently 6 spaces for `<div id="search-wrap">` at line 18 of index.html; the new `<div id="pills-wrap">` sits at the same 6-space level).
  </action>
  <verify>
    <automated>
Open the page locally (`python -m http.server 8000` then visit http://localhost:8000) and confirm in the browser:
1. The `#pills-wrap` block renders inside `#controls`, below the search input.
2. 10 pills are visible in order: Name, Wage, HP, MSk, RSk, MDf, RDf, Fat, Res, Ini.
3. The Name pill is visibly amber-filled (active state) and shows a ▲ glyph to the right of "Name".
4. The other 9 pills render as unfilled pill outlines.
5. On a narrow viewport (resize to < 430px), `#pills-wrap` scrolls horizontally; the scrollbar is hidden.
6. `#controls` (including both `#search-wrap` and `#pills-wrap`) is sticky — scrolling the background list keeps both rows pinned to the top of `#app`.
7. Tabbing through the page: focus moves through the search input, then through all 10 pills in order (Name → Wage → … → Ini); each focused pill shows a visible focus indicator.
8. DevTools → Elements: each pill is a `<button>` with the correct `data-key` value and `aria-pressed="true|false"`.
9. No JS errors in the console — clicking a pill currently does nothing (correct for this plan; wiring arrives in Plan 06-02).
10. Page still renders all 67 backgrounds and the result count still reads `67 of 67 backgrounds`.

No code-level test runner exists in this project (CODEBASE.md §2 — "Testing conventions: None"). Manual visual verification is the established pattern.
    </automated>
  </verify>
  <acceptance_criteria>
- `#pills-wrap > #pills` is inserted inside `#controls` as a sibling of `#search-wrap`, AFTER `#search-wrap` (per D-07).
- `#pills` contains EXACTLY 10 `<button class="pill" data-key="…">` children in this order: name, baseWage, hitpoints, meleeSkill, rangedSkill, meleeDefense, rangedDefense, fatigue, resolve, initiative (D-08, D-09).
- The Name pill has `class="pill active"`, `aria-pressed="true"`, and a `<span class="arrow">▲</span>` child (literal U+25B2 character) — D-10, D-29.
- The other 9 pills have `class="pill"` (NO `.active`), `aria-pressed="false"`, and NO `<span class="arrow">` child — D-10.
- Every pill carries `aria-pressed="true|false"` reflecting its active state (D-14).
- No per-pill `aria-label` added (D-15). No `aria-live` on `#result-count` (out of scope).
- No `onclick=` inline handler, no `tabindex`, no wrapper divs beyond `#pills-wrap > #pills`.
- Page still renders all 67 backgrounds via the existing Phase 5 applyFilter pipeline; result count reads `67 of 67 backgrounds`; no JS errors.
- Visual check confirms sticky positioning, amber-filled Name pill, hidden horizontal scrollbar on `#pills-wrap`, visible focus indicator when tabbing to a pill.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Reserve sortKey and sortAsc module-scope state in app.js</name>
  <read_first>
    - app.js (lines 1–20 — IIFE opening, state block, D-19 Phase 5 additions)
    - .planning/phases/06-sort-pills-direction/06-CONTEXT.md (D-19, D-20, plus the comment-header convention note)
    - .planning/CODEBASE.md §2 ("Code style" + "Double-fence comment blocks") and §4.1 ("State additions")
  </read_first>
  <files>app.js</files>
  <action>
Append two new module-scope `let` declarations to the existing state block in `app.js`, immediately AFTER the Phase 5 D-19 additions (currently `let filtered = [];` and `let query = '';` at lines 14–15).

Wrap the additions in a Phase-6-specific comment header following the project's existing `// ── Phase N D-NN: … ──` convention (CODEBASE.md §2). Add EXACTLY:

```javascript
  // ── Phase 6 D-19: sort state (consumed by applySort + pill handler in 06-02) ──
  let sortKey = 'name';
  let sortAsc = true;
```

Constraints:
- Insert these three lines (comment + 2 `let`s) AFTER `let query = '';` (current line 15) and BEFORE the blank line that precedes `const DATA_URL` (current line 17). Maintain the existing 2-space IIFE indent.
- Initial values MUST be `sortKey = 'name'` and `sortAsc = true` — these match the hard-coded Name-active preset in index.html (D-10) and the `sortAsc = key === 'name'` default-direction rule from D-01.
- Do NOT add `sortValue`, `applySort`, the pill-click handler, or the `__setSort` dev hook in this task. This task is state-reservation ONLY — Plan 06-02 lands the pipeline, the handler, and the dev hook.
- Do NOT modify, rename, or reorder any existing state variable (`allBgs`, `openId`, `filtered`, `query`, etc.).
- Do NOT change the existing `// ── Module state ─────────────────────────────────────────────` header or the `// Phase 6 state (sortKey, sortAsc) intentionally absent.` comment above. The new header sits BELOW the Phase 5 D-19 block, demarcating the Phase-6 addition.

Optional discretion (per CODEBASE.md §2 comment-header accuracy): The line 5 comment currently reads `// Phase 6 state (sortKey, sortAsc) intentionally absent.` — this becomes stale once you add sortKey/sortAsc. You MAY update it to `// All module state for v2.0 Search & Sort is now declared (Phases 5 + 6).` OR simply delete it. If unsure, leave it alone — Plan 06-02 may revisit. Either choice is fine; no behavior impact.
  </action>
  <verify>
    <automated>
Reload the page (`python -m http.server 8000` then http://localhost:8000) and confirm:
1. The page still loads without JS errors — open DevTools Console, see no red errors.
2. All 67 backgrounds render; the result count reads `67 of 67 backgrounds`.
3. Open DevTools Sources tab, navigate to app.js, confirm the new lines are present:
   - `// ── Phase 6 D-19: sort state (consumed by applySort + pill handler in 06-02) ──`
   - `let sortKey = 'name';`
   - `let sortAsc = true;`
   All immediately after `let query = '';` and before `const DATA_URL`.
4. Run `node -c app.js` (from repo root, on any shell with node installed) — returns silently (syntax OK).
5. Clicking a pill still does nothing (correct — handler arrives in Plan 06-02).

No runtime behavior change is expected — these are unused state slots until Plan 06-02 wires them.
    </automated>
  </verify>
  <acceptance_criteria>
- `app.js` contains `let sortKey = 'name';` and `let sortAsc = true;` declared at IIFE module scope, after `let query = '';` and before the `const DATA_URL` line.
- A `// ── Phase 6 D-19: sort state (consumed by applySort + pill handler in 06-02) ──` comment header demarcates the addition (matching project convention).
- Initial values are exactly `'name'` (string) and `true` (boolean) — matches D-01 / D-10 / D-19.
- `sortValue`, `applySort`, the pill-click handler, and `window.__setSort` are NOT added.
- All existing state declarations and constants remain untouched (same names, same initial values, same order).
- `node -c app.js` passes (syntax OK).
- Page loads without JS errors; all 67 backgrounds still render.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Add the single new .pill:focus-visible CSS rule to styles.css</name>
  <read_first>
    - styles.css (lines 1–145 — :root tokens through existing pill rules)
    - .planning/phases/06-sort-pills-direction/06-CONTEXT.md (D-18, D-27, D-28)
    - .planning/CODEBASE.md §4.2 (CSS sufficiency statement and the `.pill:focus-visible` caveat)
  </read_first>
  <files>styles.css</files>
  <action>
Add EXACTLY ONE new CSS rule to `styles.css`, inserted immediately AFTER the existing `.pill .arrow { … }` rule (currently ends at styles.css line 140) and BEFORE the next rule block. This keeps all pill-related selectors contiguous.

The new rule, verbatim:

```css
  .pill:focus-visible {
    outline: 2px solid var(--amber);
    outline-offset: 2px;
  }
```

Indentation: Match the existing pill rule indentation in styles.css (2-space indent for selectors nested inside the wrapping root block — verify against the `.pill { … }` rule at line 110).

Critical discipline (per D-18, D-27, D-28):
- Use `var(--amber)` (defined in `:root` at styles.css line 9 as `#d4a843`). Do NOT use `var(--amber-dim)`, do NOT hard-code `#d4a843`, do NOT add a new `:root` token.
- Use `outline` (NOT `border`) so it does not affect the pill's box layout or shift the text.
- `outline-offset: 2px` gives breathing room between the pill border and the focus ring (consistent with common a11y patterns).
- This is the ONLY CSS rule this phase adds (D-27). Do NOT modify any existing rule. Do NOT add rules for `.pill:hover`, `.pill:focus` (non-visible), `#controls` height, or any other selector. Do NOT change `:root` tokens (D-28).
- Do NOT touch the unused `--ctrl-h: 88px` token (CODEBASE.md §4.5 — harmless, leave as-is).

The rule targets `:focus-visible` (not `:focus`) so mouse clicks do NOT show the ring; only keyboard focus does. This is modern default a11y behavior supported in all evergreen browsers.
  </action>
  <verify>
    <automated>
Reload the page (`python -m http.server 8000` then http://localhost:8000) and confirm:
1. Tab through the page from the URL bar: focus moves to the search input, then into `#pills` starting with the Name pill, then through Wage → HP → … → Ini in order.
2. Each focused pill displays a visible 2px amber outline with 2px offset — the ring sits just outside the pill's normal border without shifting the pill's position.
3. Clicking a pill with the mouse does NOT show the focus ring (`:focus-visible` semantics — mouse-focus is suppressed).
4. DevTools → Elements → Computed styles on a tab-focused pill shows `outline: rgb(212, 168, 67) solid 2px;` (or equivalent) and `outline-offset: 2px`.
5. No other visual regression on pills — amber-filled active state still renders, hover/active transitions still work.
6. Page still renders all 67 backgrounds; no JS errors.

No code-level test runner exists (CODEBASE.md §2). Manual visual + DevTools verification is the established pattern.
    </automated>
  </verify>
  <acceptance_criteria>
- `styles.css` contains a new `.pill:focus-visible { outline: 2px solid var(--amber); outline-offset: 2px; }` rule immediately after the `.pill .arrow` rule at line ~140.
- The rule uses `var(--amber)` (not `--amber-dim`, not a hard-coded hex).
- The rule uses `outline` (not `border`) — no box-layout shift on focus.
- No other CSS edits: no new `:root` tokens, no modifications to existing `.pill`, `#pills-wrap`, `#controls`, `.pill.active`, `.pill .arrow`, or any other rule.
- Tab-focus on a pill shows the amber outline; mouse click does NOT (`:focus-visible` semantics).
- Page still renders all 67 backgrounds; no JS or CSS errors in the console.
  </acceptance_criteria>
</task>

</tasks>

<verification>
Phase 6 Plan 1 success requires:

1. **DOM parity check:** Open `index.html` in a browser. The `#pills-wrap > #pills` block renders inside `#controls` as a sibling AFTER `#search-wrap`. 10 pills appear in order (Name, Wage, HP, MSk, RSk, MDf, RDf, Fat, Res, Ini). The Name pill is amber-filled with a ▲ glyph; the other 9 are unfilled outlines. Compare against `mockups/design3_accordion.html` rendered in a second browser tab — the pills row should be visually identical.

2. **Attribute parity check:** DevTools confirms each `<button class="pill">` has the correct `data-key` value AND `aria-pressed` attribute. The Name pill alone has `aria-pressed="true"`; the other 9 have `aria-pressed="false"`. The ▲ glyph is a literal U+25B2 character inside a `<span class="arrow">` child on the Name pill only.

3. **CSS activation check:** The dormant rules in `styles.css` (lines 96–140) light up — horizontal scroll with hidden scrollbar on `#pills-wrap`, `display: flex; width: max-content` on `#pills`, pill-shape outlines on `.pill`, amber fill on `.pill.active`, 9px arrow on `.pill .arrow`. The new `.pill:focus-visible` rule produces an amber outline on keyboard focus (mouse-focus suppressed).

4. **JS state check:** `app.js` declares `sortKey = 'name'` and `sortAsc = true` at IIFE scope; no behavior change yet (clicking a pill does nothing — correct for Plan 1 scope). `node -c app.js` passes.

5. **Regression check:** All 67 backgrounds still render; the accordion still expands/collapses; the result count still reads `67 of 67 backgrounds`; the search input still filters with the 180 ms debounce (Phase 5 pipeline unaffected); no JS errors in the console.

6. **First-paint parity (SC-2 partial):** On initial page load, the Name pill visibly has the `.active` state + ▲ glyph BEFORE any JS runs (verified by viewing page source or disabling JS and reloading) — eliminates first-paint flicker per D-10.
</verification>

<success_criteria>
- `index.html` contains `#pills-wrap > #pills` as a sibling of `#search-wrap` inside `#controls`, with 10 `<button class="pill" data-key="…" aria-pressed="…">` children in the exact order Name, Wage, HP, MSk, RSk, MDf, RDf, Fat, Res, Ini (D-07, D-08, D-09).
- The Name pill is preset to `class="pill active"`, `aria-pressed="true"`, and contains a `<span class="arrow">▲</span>` child (literal U+25B2) — D-10, D-29.
- The other 9 pills are preset to `class="pill"`, `aria-pressed="false"`, no `<span class="arrow">` child — D-10.
- `app.js` declares `let sortKey = 'name'` and `let sortAsc = true` at module scope under a Phase 6 D-19 comment header (D-19, D-20).
- `styles.css` contains exactly ONE new rule: `.pill:focus-visible { outline: 2px solid var(--amber); outline-offset: 2px; }` (D-18, D-27).
- No other CSS edits (D-27). No `:root` token changes (D-28).
- No sort helpers, handlers, or dev hooks added (Plan 06-02 scope).
- Page still renders all 67 backgrounds via the Phase 5 applyFilter pipeline; result count reads `67 of 67`; no JS errors.
- Success Criteria SC-1 (visible pill row with horizontal scroll + hidden scrollbar) and SC-2 (Name pill active + ▲ on first load — visual only; sort behavior arrives in Plan 06-02) are partially delivered by this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/06-sort-pills-direction/06-01-SUMMARY.md` documenting:
- Files modified (index.html, app.js, styles.css)
- Exact line counts / line ranges for each insertion
- Decisions implemented (D-07, D-08, D-09, D-10, D-14, D-15, D-17, D-18, D-19, D-20, D-27, D-28, D-29)
- The one intentional deviation from the mockup (`aria-pressed="true|false"` on every pill, justified by D-14 — new DOM)
- Confirmation that NO sort helpers, NO pill-click handler, and NO `__setSort` dev hook were added (Plan 06-02 scope)
- Hand-off note for Plan 06-02: "Pills DOM is live with Name-active preset; `sortKey='name'` / `sortAsc=true` state is reserved; `.pill:focus-visible` rule shipped. Plan 06-02 adds `sortValue()`, `applySort()`, wires `applySort()` into `applyFilter()`, extends `wireControls()` with the delegated pill-click handler, adds `window.__setSort`, and adds one `applyFilter()` call at end of `load()` success branch (D-26)."
</output>
</content>
</invoke>