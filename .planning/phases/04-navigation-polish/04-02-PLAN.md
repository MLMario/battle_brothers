---
phase: 04-navigation-polish
plan: 02
type: execute
requirements: [NAV-01, NAV-03]
wave: 2
depends_on: [04-01]
files_modified: [app.js]
autonomous: false

must_haves:
  truths:
    - "After fetch resolves, #result-count displays the text '67 of 67 backgrounds' exactly"
    - "During fetch (before resolution), #result-count textContent is empty (no '0 of 0' flash)"
    - "Calling window.__setEmpty(true) shows #empty and hides #list"
    - "Calling window.__setEmpty(false) hides #empty and restores #list visibility"
    - "Error state (fetch fails) does not populate #result-count and does not toggle #empty"
  artifacts:
    - path: "app.js"
      provides: "updateCount(filtered, total) helper writing to #result-count"
      contains: "function updateCount"
    - path: "app.js"
      provides: "setEmpty(show) helper toggling #empty/#list visibility"
      contains: "function setEmpty"
    - path: "app.js"
      provides: "window.__setEmpty verification hook"
      contains: "window.__setEmpty"
  key_links:
    - from: "app.js fetch .then handler"
      to: "#result-count textContent"
      via: "updateCount(allBgs.length, allBgs.length) call after renderList"
      pattern: "updateCount\\("
    - from: "app.js setEmpty(show)"
      to: "#empty.style.display and #list.style.display"
      via: "direct style.display toggle"
      pattern: "setEmpty"
    - from: "window.__setEmpty"
      to: "setEmpty function reference"
      via: "global assignment"
      pattern: "window\\.__setEmpty\\s*="
---

<objective>
Wire the Phase 4 markup from Plan 04-01 to real behavior. Define `updateCount(filtered, total)` and `setEmpty(show)` helpers in `app.js`, call `updateCount(allBgs.length, allBgs.length)` after the fetch resolves, and expose `window.__setEmpty = setEmpty` for verification (D-12). Closes v1: NAV-01 (result count), NAV-03 (empty state). NAV-02 is fully satisfied by Plan 04-01's static nav markup (no JS needed, per D-16).

Purpose: Complete the three active v1 requirements and bring REQUIREMENTS.md coverage to 13/13.
Output: Updated `app.js` with two new helpers and one integration point in the fetch pipeline.
</objective>

<execution_context>
@~/.claude/gsd-light/workflows/execute-plan.md
@~/.claude/gsd-light/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-navigation-polish/04-CONTEXT.md
@.planning/phases/04-navigation-polish/04-01-PLAN.md
@app.js
@index.html

<interfaces>
<!-- Current app.js shape (IIFE, 456 lines): -->
<!--   - State: allBgs, globalMin/Max, openId, ATTR_KEYS, constants. -->
<!--   - Helpers: iconUrl, wireIconFallback, computeGlobalMinMax, pct, barColor, makeSparkline, -->
<!--     renderSkeleton, renderError, formatLevelUp, buildAttrRow, buildPanel, toggleItem, buildRow, renderList. -->
<!--   - Lifecycle: load() does fetch → success path calls renderList(allBgs); error path calls renderError(). -->
<!--   - Entry: DOMContentLoaded → load(). -->
<!-- -->
<!-- NEW helpers to add (top-level inside the IIFE, near the other helpers): -->
<!--   function updateCount(filtered, total) { -->
<!--     const el = document.getElementById('result-count'); -->
<!--     if (!el) return; -->
<!--     el.textContent = filtered + ' of ' + total + ' backgrounds'; -->
<!--   } -->
<!-- -->
<!--   function setEmpty(show) { -->
<!--     const emptyEl = document.getElementById('empty'); -->
<!--     const listEl  = document.getElementById('list'); -->
<!--     if (!emptyEl || !listEl) return; -->
<!--     emptyEl.style.display = show ? 'flex' : 'none'; -->
<!--     listEl.style.display  = show ? 'none' : ''; -->
<!--   } -->
<!-- -->
<!--   window.__setEmpty = setEmpty;  // D-12 — verification hook, unconditional (D-13) -->
<!-- -->
<!-- Integration point: inside load() .then(success) branch, AFTER renderList(allBgs), -->
<!-- add: updateCount(allBgs.length, allBgs.length);  // D-07/D-08 -->
<!-- -->
<!-- Do NOT call updateCount during skeleton/loading (D-09 — element stays empty). -->
<!-- Do NOT call updateCount in the .catch (renderError) branch (D-09 — empty during error too). -->
<!-- Do NOT call setEmpty from v1 code paths — only the dev hook exposes it (D-11/D-12). -->
<!-- Use `listEl.style.display = ''` (not 'block' or 'flex') so the CSS rule from styles.css:151 -->
<!-- wins — #list is a flex child of #app with flex: 1. Hardcoding 'block' would break layout. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add updateCount + setEmpty helpers and wire into fetch lifecycle</name>
  <read_first>app.js (entire file, 456 lines), index.html (verify #result-count, #empty, #list exist from Plan 04-01), .planning/phases/04-navigation-polish/04-CONTEXT.md (D-06 through D-13)</read_first>
  <files>app.js</files>
  <action>
Edit `app.js`. Three changes, inside the existing IIFE.

**Change 1: Add `updateCount` helper** (D-07). Place it near the other small DOM helpers (e.g., after `wireIconFallback` or before `renderList`). Signature: `function updateCount(filtered, total)`. Implementation: find `#result-count` via `document.getElementById('result-count')`; if missing, return silently (defensive — matches the pattern used by `renderSkeleton`/`renderError`). Otherwise set `textContent` to `filtered + ' of ' + total + ' backgrounds'`. Use string concatenation to match the file's existing style.

**Change 2: Add `setEmpty` helper + `window.__setEmpty` hook** (D-11, D-12, D-13). Place immediately after `updateCount`. Signature: `function setEmpty(show)`. Implementation:
- Look up `#empty` and `#list`.
- If either is missing, return silently.
- When `show === true`: set `emptyEl.style.display = 'flex'` (matches the CSS `flex-direction: column` at styles.css:429 which expects `flex`) and `listEl.style.display = 'none'`.
- When `show === false` (or falsy): set `emptyEl.style.display = 'none'` and `listEl.style.display = ''` (empty string — lets the stylesheet rule reassert `flex: 1; overflow-y: auto` from styles.css:151 rather than hardcoding a value).
- Immediately after the function definition, add `window.__setEmpty = setEmpty;` as an unconditional top-level assignment (D-12 — no DEV guard, no conditional).

**Change 3: Wire `updateCount` into the fetch success path** (D-07, D-08). In the existing `load()` function's `.then(function (data) {...})` branch, AFTER the existing `renderList(allBgs);` line (currently line ~435), add:
```js
updateCount(allBgs.length, allBgs.length);
```
Do NOT add this call anywhere in `renderSkeleton`, `renderError`, or the `.catch(...)` branch — per D-09 the element must be empty during loading and error states, which is automatically the case since `#result-count` starts with empty textContent from Plan 04-01's markup.

Do NOT call `setEmpty(false)` anywhere in v1 code paths — the CSS default (`#empty { display: none }` at styles.css:427) already handles the initial hidden state, and there is no filter in v1 that could produce zero rows (D-11 — only the dev hook uses it).

Do NOT modify `renderList`, `renderError`, `renderSkeleton`, or any Phase 1-3 helper's internals. Do NOT add click handlers to `.nav-tab` (D-16). Do NOT touch `styles.css`.

Preserve the file's existing style: 2-space indent, `'use strict';`, function declarations (not arrow functions), semicolons, double-underscore prefix for dev-only globals.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('app.js','utf8');const checks=[/function updateCount\s*\(/, /function setEmpty\s*\(/, /window\.__setEmpty\s*=\s*setEmpty/, /updateCount\s*\(\s*allBgs\.length\s*,\s*allBgs\.length\s*\)/, /result-count/, /getElementById\(\s*['\"]empty['\"]\s*\)/];const fail=checks.filter(r=>!r.test(s));if(fail.length){console.error('MISSING:',fail.map(r=>r.toString()));process.exit(1);}console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `app.js` defines `function updateCount(filtered, total)` that writes `${filtered} of ${total} backgrounds` to `#result-count.textContent`
    - `app.js` defines `function setEmpty(show)` that toggles `#empty.style.display` between `'flex'` and `'none'` and `#list.style.display` between `'none'` and `''`
    - `app.js` contains the unconditional statement `window.__setEmpty = setEmpty;`
    - `app.js` `load()` calls `updateCount(allBgs.length, allBgs.length)` inside the fetch `.then` success branch, after `renderList(allBgs)`
    - `app.js` does NOT call `updateCount` from `renderSkeleton`, `renderError`, or the fetch `.catch` branch
    - `app.js` does NOT call `setEmpty` from any production code path (only exposed via `window.__setEmpty`)
    - No modifications to `renderList`, `renderError`, `renderSkeleton`, `buildRow`, or any Phase 1-3 helper internals
    - No modifications to `styles.css` or `index.html`
  </acceptance_criteria>
  <done>All acceptance_criteria satisfied and automated verify prints OK.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of result-count and empty-state toggle</name>
  <action>Developer opens the app in a browser and runs through the verification steps below. No code changes performed in this task.</action>
  <what-built>Wired `updateCount` and `setEmpty` helpers into `app.js`. Result count populates after data loads; empty-state toggle is exposed via `window.__setEmpty` for verification.</what-built>
  <how-to-verify>
1. Open `index.html` in a browser (preferably via `python -m http.server` to keep fetch working).
2. After the list loads, confirm `#result-count` at the top of the list reads exactly **`67 of 67 backgrounds`** (no extra whitespace, no "0 of 0" flash during load). DevTools check: `document.getElementById('result-count').textContent === '67 of 67 backgrounds'` → `true`.
3. Reload with Network throttling set to "Slow 3G". Confirm that during the skeleton-loading phase `#result-count` is empty (no text flash). Once data resolves, text appears.
4. Simulate error: temporarily break the fetch URL (DevTools → Network → block `backgrounds.json` or rename the file). Reload. Confirm the error state renders in `#list` AND `#result-count` stays empty (no stale or zero text).
5. Restore the data file. Reload to a clean state.
6. In DevTools console: `window.__setEmpty(true)`. Confirm:
   - `#empty` appears centered showing the magnifying-glass SVG and "No backgrounds found" text.
   - `#list` is hidden (accordion rows no longer visible).
   - `#result-count` still shows "67 of 67 backgrounds" (the helper does not clear it — v2 search will re-call updateCount).
   - `#bottom-nav` is still visible at the bottom.
7. In DevTools console: `window.__setEmpty(false)`. Confirm:
   - `#empty` disappears.
   - `#list` returns and all 67 accordion rows are visible again and scrollable.
   - Clicking a row still expands/collapses it (Phase 3 behavior intact).
8. Side-by-side vs the mockup: nav bar visual (icons, active underline, amber accent), empty state text + icon, result-count position and typography all match.
  </how-to-verify>
  <verify>Manual browser inspection per how-to-verify steps above.</verify>
  <acceptance_criteria>Developer types "approved" after all 8 verification steps pass.</acceptance_criteria>
  <done>Developer approval captured.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `updateCount(67, 67)` produces `"67 of 67 backgrounds"` exactly (NAV-01).
- `window.__setEmpty(true)` / `window.__setEmpty(false)` toggles `#empty`/`#list` with no flicker or layout break (NAV-03).
- Bottom nav shipped in Plan 04-01 remains visible during loading, success, and error states (NAV-02).
- All 13 v1 requirements now satisfied — roadmap Phase 4 success criterion 5 met.
</verification>

<success_criteria>
- Task 1 acceptance_criteria met; automated verify passes.
- Human-verify checkpoint confirms both helpers work and no Phase 1-3 regressions.
- REQUIREMENTS.md coverage reaches 13/13 after this plan ships and Phase 4 verifies.
</success_criteria>

<output>
After completion, create `.planning/phases/04-navigation-polish/04-02-SUMMARY.md` documenting the two new helpers, the fetch-pipeline integration point, and confirming v1 is closed (13/13 requirements).
</output>
