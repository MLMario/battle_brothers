---
phase: 04-navigation-polish
plan: 01
type: execute
requirements: [NAV-01, NAV-02, NAV-03]
wave: 1
depends_on: [03]
files_modified: [index.html]
autonomous: false

must_haves:
  truths:
    - "#result-count element exists as a sibling of #list, before #list, inside #app"
    - "#empty element exists as a sibling of #list inside #app with the mockup SVG + 'No backgrounds found' text"
    - "#bottom-nav element exists as a sibling of #list inside #app with two .nav-tab children (Backgrounds active, Build placeholder)"
    - "Both .nav-tab inline SVGs render verbatim from mockup lines 498-503 (Backgrounds/people icon) and 507-511 (Build/card icon)"
    - "#list-wrap is NOT reintroduced (Phase 3 gap-fix stays intact — #list remains the direct scroll container)"
  artifacts:
    - path: "index.html"
      provides: "Phase 4 DOM scaffold — result count, list, empty, bottom-nav as siblings inside #app"
      contains: "id=\"result-count\""
    - path: "index.html"
      provides: "Empty state markup — SVG + text verbatim from mockup"
      contains: "No backgrounds found"
    - path: "index.html"
      provides: "Bottom navigation — Backgrounds (active) + Build (placeholder)"
      contains: "id=\"bottom-nav\""
  key_links:
    - from: "index.html #app"
      to: "#result-count, #list, #empty, #bottom-nav"
      via: "sibling DOM children of #app"
      pattern: "<div id=\"app\">"
    - from: "index.html .nav-tab.active"
      to: "styles.css:406 .nav-tab.active rule"
      via: "CSS class match"
      pattern: "nav-tab active"
---

<objective>
Add the three Phase 4 UI elements to `index.html` as markup only — a `#result-count` element above `#list`, an `#empty` state element alongside `#list`, and a `#bottom-nav` with two `.nav-tab` children. All CSS is already in `styles.css` (D-01 context). No JS wiring in this plan.

Purpose: Lay the DOM scaffold that Plan 04-02 will drive via `updateCount` and `setEmpty` helpers. Complete the v1 chrome.
Output: Updated `index.html` with 4 new sibling elements inside `#app`.
</objective>

<execution_context>
@~/.claude/gsd-light/workflows/execute-plan.md
@~/.claude/gsd-light/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-navigation-polish/04-CONTEXT.md
@index.html
@mockups/design3_accordion.html
@styles.css

<interfaces>
<!-- Current index.html shape (20 lines) — the only element inside #app is `<main id="list">`. -->
<!-- The four siblings MUST be added in this exact order inside #app: -->
<!--   1. #result-count (before #list) -->
<!--   2. #list (unchanged — keep role="list" aria-label) -->
<!--   3. #empty (after #list, display:none by default per styles.css:427) -->
<!--   4. #bottom-nav (after #empty; position:fixed via styles.css:373 so DOM position is flexible but keep it last for reading order) -->
<!-- -->
<!-- Mockup source (copy verbatim): -->
<!--   #result-count: mockup line 482 → `<div id="result-count"></div>` -->
<!--   #empty block:  mockup lines 487-492 (SVG circle + line, "No backgrounds found") -->
<!--   #bottom-nav:   mockup lines 496-514 (two .nav-tab divs with inline SVGs) -->
<!-- -->
<!-- CRITICAL: Do NOT reintroduce #list-wrap (D-01). #list stays as the direct scroll container -->
<!-- with its padding-bottom: calc(var(--nav-h) + 4px) untouched. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add #result-count, #empty, and #bottom-nav markup to index.html</name>
  <read_first>index.html, mockups/design3_accordion.html (lines 480-515), .planning/phases/04-navigation-polish/04-CONTEXT.md, styles.css (lines 143-148, 373-438)</read_first>
  <files>index.html</files>
  <action>
Modify `index.html` inside the `<div id="app">` block. Current state: `<main id="list" role="list" aria-label="Backgrounds"></main>` is the only child of `#app`.

Add three new sibling elements per D-02, D-03, D-04, and D-14/D-15:

1. **BEFORE `#list`**, insert `<div id="result-count"></div>` verbatim from mockup line 482. Empty textContent — Plan 04-02 populates it.

2. **AFTER `#list`**, insert the empty-state block verbatim from mockup lines 487-492:
```html
<div id="empty">
  <svg width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
    <circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/>
  </svg>
  No backgrounds found
</div>
```
No wrapper `#list-wrap` (per D-01 — Phase 3 gap-fix stays intact). `#empty` is a direct sibling of `#list`.

3. **AFTER `#empty`**, insert the bottom nav verbatim from mockup lines 496-514:
```html
<nav id="bottom-nav">
  <div class="nav-tab active">
    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round">
      <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/>
      <circle cx="9" cy="7" r="4"/>
      <path d="M23 21v-2a4 4 0 0 0-3-3.87"/>
      <path d="M16 3.13a4 4 0 0 1 0 7.75"/>
    </svg>
    Backgrounds
  </div>
  <div class="nav-tab">
    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round">
      <rect x="2" y="3" width="20" height="14" rx="2" ry="2"/>
      <line x1="8" y1="21" x2="16" y2="21"/>
      <line x1="12" y1="17" x2="12" y2="21"/>
    </svg>
    Build
  </div>
</nav>
```

Do NOT add click handlers, `cursor: not-allowed`, `aria-*`, `title`, or any other attributes to `.nav-tab` (D-16 — no interactivity).
Do NOT add `#list-wrap` (D-01).
Do NOT add `#controls`/`#search`/`#pills` (D-05 — deferred to v2).
Do NOT write any CSS — all needed rules already exist in `styles.css` (lines 143-148, 373-438, 427-438).
Keep `<main id="list" role="list" aria-label="Backgrounds"></main>` exactly as-is — do not change its tag or attributes.

Preserve existing indentation style (2 spaces) to match surrounding code.
  </action>
  <verify>
    <automated>grep -q 'id="result-count"' index.html &amp;&amp; grep -q 'id="empty"' index.html &amp;&amp; grep -q 'id="bottom-nav"' index.html &amp;&amp; grep -q 'class="nav-tab active"' index.html &amp;&amp; grep -q 'No backgrounds found' index.html &amp;&amp; ! grep -q 'id="list-wrap"' index.html &amp;&amp; ! grep -q 'id="controls"' index.html &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `index.html` contains `<div id="result-count"></div>` before `<main id="list"`
    - `index.html` contains `<div id="empty">` with the magnifying-glass SVG and "No backgrounds found" text
    - `index.html` contains `<nav id="bottom-nav">` with exactly two `.nav-tab` children (first has `.active` class, second does not)
    - Both `.nav-tab` SVGs match mockup lines 498-503 (Backgrounds/people) and 507-511 (Build/card-window) verbatim
    - `#list-wrap` is NOT present (D-01)
    - `#controls`, `#search`, `#pills` are NOT present (D-05)
    - No click handlers, `aria-*`, or `title` attributes on `.nav-tab` elements
    - `<main id="list" role="list" aria-label="Backgrounds">` is unchanged
  </acceptance_criteria>
  <done>All acceptance_criteria above satisfied and automated verify command prints OK.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of Phase 4 markup</name>
  <action>Developer opens index.html in a browser and visually confirms the new chrome renders correctly without regressing Phase 1-3 behavior. No code changes performed in this task — verification only.</action>
  <what-built>Static DOM chrome for Phase 4: result-count slot, empty state, and bottom nav bar in index.html. No JS wiring yet — the result-count is empty text, the empty state is hidden by default CSS, and the bottom nav renders immediately.</what-built>
  <how-to-verify>
1. Open `index.html` in a browser (no local server needed; file:// works, but `python -m http.server` is recommended to keep fetch happy).
2. The page loads normally — 67 accordion rows render inside `#list` (Phase 1-3 behavior unchanged).
3. Scroll the list — it scrolls smoothly above the bottom nav. The nav does not overlap the last row (padding-bottom on `#list` is already `calc(var(--nav-h) + 4px)`).
4. Verify the bottom nav is visible at the bottom of the 430px column, showing:
   - Left tab: people icon in amber (`#d4a843`) with underline bar on top + "Backgrounds" text in amber (active state).
   - Right tab: card/window icon in grey (`#555`) + "Build" text in grey (placeholder state).
5. Tap/click either nav tab — nothing happens (D-16, no handlers). Confirm neither shows a toast, navigates, or errors.
6. Open DevTools console and run: `document.getElementById('empty').style.display = 'flex'` — the empty state appears (SVG magnifying-glass + "No backgrounds found"). `#list` still shows behind it (toggle wiring comes in Plan 04-02). Reset with `document.getElementById('empty').style.display = 'none'`.
7. `#result-count` is empty (no visible text at top of list) — Plan 04-02 will populate it.
8. Confirm no `#list-wrap` exists: `document.getElementById('list-wrap') === null` in console.
  </how-to-verify>
  <verify>Manual browser inspection per how-to-verify steps above.</verify>
  <acceptance_criteria>Developer types "approved" after completing all 8 verification steps without issues.</acceptance_criteria>
  <done>Developer approval captured.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `index.html` gains 3 new siblings inside `#app` (#result-count, #empty, #bottom-nav) with markup matching the mockup verbatim.
- Bottom nav renders with correct active/inactive styling (CSS already in place).
- Empty state is hidden by default (CSS default `display:none`) and can be forced visible for manual inspection.
- Phase 3 scroll container structure (#list as direct scroll host) is preserved — no #list-wrap.
</verification>

<success_criteria>
- All acceptance_criteria on Task 1 met.
- Human-verify checkpoint passes on visual inspection.
- No regressions to Phase 1-3 accordion behavior (data loads, rows render, expand/collapse works).
</success_criteria>

<output>
After completion, create `.planning/phases/04-navigation-polish/04-01-SUMMARY.md` documenting the markup added and noting that JS wiring lands in Plan 04-02.
</output>
