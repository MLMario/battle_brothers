---
phase: 05-search-filter
plan: 01
type: execute
requirements: [SRCH-01-c1]
wave: 1
depends_on: []
files_modified:
  - index.html
  - app.js
autonomous: true
must_haves:
  truths:
    - "A search input is visible at the top of #app, above #result-count, sticky to the #app top edge when the list scrolls."
    - "The search input renders with the mockup's exact attributes (type=search, autocomplete=off, spellcheck=false, placeholder='Search backgrounds…')."
    - "A 14×14 magnifying-glass SVG sits inside #search-wrap as the icon, positioned over the input's left padding."
    - "The search input is keyboard-focusable and announces 'Search backgrounds' to assistive tech via aria-label."
    - "Module-scope state `filtered` and `query` exist alongside `openId` in app.js, ready for Plan 2 to wire."
    - "The shipped page still loads, renders all 67 backgrounds, and `#controls` is the first visible child of #app."
  artifacts:
    - path: "index.html"
      provides: "#controls > #search-wrap (icon + input) inserted as first visible child of #app, before #result-count"
      contains: "id=\"controls\""
    - path: "app.js"
      provides: "Module-scope state additions: `let filtered = [];` and `let query = '';` after line 12; no behavior change yet"
      contains: "let filtered"
  key_links:
    - from: "index.html #app"
      to: "#controls"
      via: "first child placement before #result-count (sticky positioning depends on this order)"
      pattern: "<div id=\"app\">[\\s\\S]*?<div id=\"controls\""
    - from: "#controls"
      to: "styles.css #controls (already shipped, dormant)"
      via: "id selector match — activates sticky/border-bottom CSS"
      pattern: "#controls \\{"
    - from: "app.js IIFE state block"
      to: "Plan 2 applyFilter()"
      via: "module-scope `filtered` and `query` vars consumed by Plan 2's pipeline"
      pattern: "let (filtered|query)"
---

<objective>
Land the `#controls > #search-wrap` DOM scaffold inside `index.html` and add the two new module-scope state variables (`filtered`, `query`) to `app.js`. This is the structural prerequisite for Plan 05-02, which wires the filter pipeline and event handling.

Purpose: Activate the dormant CSS in `styles.css` (already shipped Phase 1) by introducing the matching DOM, and reserve the JS state slots Plan 2 will consume. Mockup-parity discipline governs every attribute and ordering decision.

Output: Updated `index.html` with the new `#controls` block; updated `app.js` with two new `let` declarations and a comment header demarcating Plan 5 scope.
</objective>

<execution_context>
@~/.claude/gsd-light/workflows/execute-plan.md
@~/.claude/gsd-light/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/CODEBASE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-search-filter/05-CONTEXT.md
@index.html
@app.js
@styles.css
@mockups/design3_accordion.html

<interfaces>
<!-- Key contracts the executor needs. Extracted directly from the shipped code and the mockup reference. -->
<!-- Use these literally — no codebase exploration needed. -->

From index.html (current shipped DOM, lines 16–44 — `#app` children in order):
```html
<div id="app">
  <div id="result-count"></div>
  <main id="list" role="list" aria-label="Backgrounds"></main>
  <div id="empty"> … </div>
  <nav id="bottom-nav"> … </nav>
</div>
```
The new `#controls` block MUST be inserted as the FIRST child of `#app`, BEFORE `#result-count` (D-02). The remaining children stay untouched.

From mockups/design3_accordion.html lines 459–465 (verbatim source for the search-wrap subtree — D-01, D-03):
```html
<div id="controls">
  <div id="search-wrap">
    <svg id="search-icon" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
      <circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/>
    </svg>
    <input id="search" type="search" placeholder="Search backgrounds…" autocomplete="off" spellcheck="false">
  </div>
</div>
```
NOTE: The mockup's `#controls` also contains `#pills-wrap > #pills` (lines 466–479). Phase 5 does NOT add the pills block — Phase 6 lands it (D-01). Ship `#controls` containing ONLY `#search-wrap` this phase.

ACCESSIBILITY ADDITION (D-11): Add `aria-label="Search backgrounds"` to the `<input id="search">` element. This is a NEW DOM addition so it does not violate the "no a11y refactor of existing v1 DOM" exclusion. The mockup does NOT have aria-label — this is the one intentional deviation, justified by D-11.

DO NOT add `aria-live="polite"` to `#result-count` (D-12 — crosses v1-DOM exclusion line).

From app.js lines 4–12 (current module state block — insertion target for D-19):
```javascript
// ── Module state (Phase 1 scope only — D-15) ─────────────────
// Phase 2+ state (filtered, sortKey, openId) intentionally absent.
let allBgs = [];
let globalMin = 0;
let globalMax = 0;
let globalMinByAttr = {};
let globalMaxByAttr = {};
let openId = null;
```
Append `let filtered = [];` and `let query = '';` AFTER `let openId = null;` (line 12), under a new comment header following the `// ── D-NN: … ──` convention (D-19, D-20). Do NOT add `sortKey` or `sortAsc` — those belong to Phase 6 (D-19).

From styles.css lines 43–94 (dormant rules that activate the moment the DOM lands — no CSS changes required, CODEBASE.md §4.2):
- `#controls` — `position: sticky; top: 0; z-index: 20`
- `#search-wrap` — `display: flex; position: relative; padding: 10px 12px 8px`
- `#search` — `flex: 1; padding: 7px 10px 7px 32px` (left padding makes room for the icon)
- `#search-icon` — `position: absolute; left: 20px; top: 50%; transform: translateY(-50%)`
- `#search:focus` — `border-color: var(--amber-dim)`
- `#search::placeholder` — `color: #444`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert #controls > #search-wrap DOM into index.html</name>
  <read_first>
    - index.html (full file, lines 1–47)
    - mockups/design3_accordion.html lines 455–480 (source-of-truth for the subtree)
    - styles.css lines 43–94 (dormant selectors that will activate)
    - .planning/phases/05-search-filter/05-CONTEXT.md (D-01, D-02, D-03, D-11)
  </read_first>
  <files>index.html</files>
  <action>
Insert a new `#controls` block as the FIRST child of `#app`, immediately before `<div id="result-count"></div>` (per D-02 — sticky positioning requires this be the first visible child).

The block MUST be exactly:

```html
    <div id="controls">
      <div id="search-wrap">
        <svg id="search-icon" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
          <circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/>
        </svg>
        <input id="search" type="search" placeholder="Search backgrounds…" autocomplete="off" spellcheck="false" aria-label="Search backgrounds">
      </div>
    </div>
```

Critical attribute discipline (per D-03 + D-11):
- `type="search"` — gives the native iOS/desktop clear "×" for free; do NOT change to `type="text"`.
- `autocomplete="off"` — prevents browser autofill suggestions.
- `spellcheck="false"` — keeps red squiggles off background names.
- `placeholder="Search backgrounds…"` — must use the horizontal-ellipsis character `…` (U+2026), NOT three ASCII dots.
- `aria-label="Search backgrounds"` — required addition (D-11). Persists for screen readers after the placeholder disappears on typing. The mockup does not have it; this is the one intentional deviation, justified by D-11 since `#search` is NEW DOM.

Do NOT add:
- A `#pills-wrap` / `#pills` block — that is Phase 6 scope (D-01).
- `aria-live="polite"` on `#result-count` (D-12 — crosses v1-DOM exclusion line).
- A custom clear button — `type="search"` provides the native one.
- Any inline styles or wrapper divs not shown above.

Indentation: Match index.html's existing 2-space indent inside `<div id="app">`. The new block sits at the same indent level as `<div id="result-count"></div>`.
  </action>
  <verify>
    <automated>
Open the page locally (`python -m http.server 8000` then visit http://localhost:8000) and confirm in the browser:
1. The `#controls` block renders at the top of `#app`, above the result count.
2. The search input shows the placeholder "Search backgrounds…" and the magnifying-glass icon overlays its left padding.
3. The input is sticky — when you scroll the background list, `#controls` stays pinned to the top of `#app`.
4. Tabbing through the page, the search input receives focus and shows the amber-dim border.
5. View source / DevTools confirms the input has `type="search"`, `autocomplete="off"`, `spellcheck="false"`, `aria-label="Search backgrounds"`.

No code-level test runner exists in this project (CODEBASE.md §2 — "Testing conventions: None"). Manual visual verification is the established pattern.
    </automated>
  </verify>
  <acceptance_criteria>
- `#controls > #search-wrap` is the FIRST child of `#app` (before `#result-count`).
- `<svg id="search-icon">` and `<input id="search" …>` are siblings inside `#search-wrap`.
- Search input attributes match exactly: `type="search"`, `autocomplete="off"`, `spellcheck="false"`, `placeholder="Search backgrounds…"`, `aria-label="Search backgrounds"`.
- The `…` in the placeholder is U+2026 (single horizontal-ellipsis character), not three ASCII dots.
- `#pills-wrap` / `#pills` is NOT added (Phase 6 scope).
- The page still renders all 67 backgrounds and the result count; nothing else in `index.html` changed.
- Visual check confirms sticky positioning, icon overlay on input, focus ring on tab.
  </acceptance_criteria>
  <done>
- `#controls > #search-wrap` is the FIRST child of `#app` (before `#result-count`).
- `<svg id="search-icon">` and `<input id="search" …>` are siblings inside `#search-wrap`.
- Search input attributes match exactly: `type="search"`, `autocomplete="off"`, `spellcheck="false"`, `placeholder="Search backgrounds…"`, `aria-label="Search backgrounds"`.
- The `…` in the placeholder is U+2026 (single horizontal-ellipsis character), not three ASCII dots.
- `#pills-wrap` / `#pills` is NOT added (Phase 6 scope).
- The page still renders all 67 backgrounds and the result count; nothing else in `index.html` changed.
- Visual check confirms sticky positioning, icon overlay on input, focus ring on tab.
  </done>
</task>

<task type="auto">
  <name>Task 2: Reserve `filtered` and `query` module-scope state in app.js</name>
  <read_first>
    - app.js (lines 1–60 — IIFE opening, state block, helpers up through `setEmpty`)
    - .planning/phases/05-search-filter/05-CONTEXT.md (D-19, D-20, plus the comment-header convention note in §specifics)
    - .planning/CODEBASE.md §2 ("Code style" + "Double-fence comment blocks") and §4.1 ("State additions")
  </read_first>
  <files>app.js</files>
  <action>
Append two new module-scope `let` declarations to the existing state block in `app.js`, immediately after `let openId = null;` (current line 12).

Wrap the additions in a Phase-5-specific comment header following the project's existing `// ── D-NN: … ──` convention (CODEBASE.md §2). Add exactly:

```javascript
  // ── Phase 5 D-19: search state (consumed by applyFilter in 05-02) ──
  let filtered = [];
  let query = '';
```

Constraints:
- Insert these two lines AFTER `let openId = null;` and BEFORE the blank line that precedes `const DATA_URL` (current line 13). Maintain the existing 2-space IIFE indent.
- Do NOT add `let sortKey` or `let sortAsc` — those belong to Phase 6 (D-19 explicitly excludes them this phase).
- Do NOT modify, rename, or reorder any existing state variable.
- Do NOT change the existing `// ── Module state (Phase 1 scope only — D-15) ─────────────────` header above. The new header sits BELOW the existing state lines, demarcating the Plan-5 addition.
- Do NOT wire any event handler, helper function, or behavior in this task. This task is state-reservation ONLY — Plan 05-02 lands the pipeline, the debounce, and the `wireControls()` call.

Note: Updating the stale `// Phase 2+ state (filtered, sortKey, openId) intentionally absent.` comment on line 5 is OPTIONAL discretion. If you touch it, change to `// Phase 6 state (sortKey, sortAsc) intentionally absent.` to keep it accurate. If left alone, no harm — Plan 2 may revisit.
  </action>
  <verify>
    <automated>
Reload the page (`python -m http.server 8000` then http://localhost:8000) and confirm in the browser DevTools console:
1. The page still loads without JS errors — open DevTools Console, see no red errors.
2. All 67 backgrounds render; the result count reads `67 of 67 backgrounds`.
3. Open DevTools Sources tab, navigate to app.js, confirm the new `let filtered = [];` and `let query = '';` lines are present after `let openId = null;`.

No runtime behavior change is expected — these are unused state slots until Plan 05-02 wires them.
    </automated>
  </verify>
  <acceptance_criteria>
- `app.js` contains `let filtered = [];` and `let query = '';` declared at IIFE module scope, after `let openId = null;` and before the `const DATA_URL` line.
- A `// ── Phase 5 D-19: search state (consumed by applyFilter in 05-02) ──` comment header demarcates the addition (matching project convention).
- `let sortKey` and `let sortAsc` are NOT added.
- All existing state declarations and constants remain untouched (same names, same initial values, same order).
- Page loads without JS errors; all 67 backgrounds still render.
  </acceptance_criteria>
  <done>
- `app.js` contains `let filtered = [];` and `let query = '';` declared at IIFE module scope, after `let openId = null;` and before the `const DATA_URL` line.
- A `// ── Phase 5 D-19: search state (consumed by applyFilter in 05-02) ──` comment header demarcates the addition (matching project convention).
- `let sortKey` and `let sortAsc` are NOT added.
- All existing state declarations and constants remain untouched (same names, same initial values, same order).
- Page loads without JS errors; all 67 backgrounds still render.
  </done>
</task>

</tasks>

<verification>
Phase 5 Plan 1 success requires:

1. **DOM parity check:** Open `index.html` in a browser. The `#controls > #search-wrap` block is the first visible child of `#app`, sits above `#result-count`, sticks to the top when the list scrolls, and renders the magnifying-glass icon over the input's left padding. Compare against `mockups/design3_accordion.html` rendered in a second browser tab — the search-wrap visual should be identical (sans pills, which arrive in Phase 6).

2. **Attribute parity check:** DevTools confirms `<input id="search">` has `type="search"`, `autocomplete="off"`, `spellcheck="false"`, `placeholder="Search backgrounds…"` (with U+2026), `aria-label="Search backgrounds"`.

3. **CSS activation check:** The dormant rules in `styles.css` (lines 43–94) light up — sticky positioning, amber-dim focus border, placeholder color `#444`, icon at `left: 20px`. No CSS edits were needed (CODEBASE.md §4.2).

4. **JS state check:** `app.js` declares `filtered` and `query` at IIFE scope; no behavior change yet (typing in the search box does nothing — that is correct for Plan 1 scope).

5. **Regression check:** All 67 backgrounds still render; the accordion still expands/collapses; the result count still reads `67 of 67 backgrounds`; no JS errors in the console.
</verification>

<success_criteria>
- `index.html` contains the `#controls > #search-wrap` block as the first child of `#app`, before `#result-count`.
- `<input id="search">` has all five required attributes (`type`, `autocomplete`, `spellcheck`, `placeholder`, `aria-label`) per D-03 + D-11.
- `app.js` declares `let filtered = []` and `let query = ''` at module scope per D-19.
- No `#pills-wrap` / `#pills` DOM was added (Phase 6 scope).
- No `sortKey` / `sortAsc` state was added (Phase 6 scope).
- No CSS edits were made (dormant rules suffice per CODEBASE.md §4.2).
- Page still renders all 67 backgrounds; no JS errors.
</success_criteria>

<output>
After completion, create `.planning/phases/05-search-filter/05-01-SUMMARY.md` documenting:
- Files modified (index.html, app.js)
- Exact line counts before/after each file
- Decisions implemented (D-01, D-02, D-03, D-11, D-12, D-13, D-14, D-19, partial D-20)
- Any deviations from the mockup or context decisions (the only intentional one is `aria-label="Search backgrounds"` per D-11)
- Hand-off note for Plan 05-02: "DOM and state slots are in place. Wire `applyFilter()`, the 180ms debounce, and the `wireControls()` call in Plan 05-02."
</output>
